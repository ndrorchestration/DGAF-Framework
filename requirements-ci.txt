# Evidence-gate toolchain pinned from successful CI observations.
# Review/update deliberately; these pins do not by themselves define application runtime compatibility.
# Python 3.9 compatibility must be evaluated separately before asserting a full supported matrix.
pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0
pytest-timeout==2.5.0
mypy==2.3.1
flake8==7.3.0
black==26.5.1
isort==8.0.1
pydantic==2.13.4
bandit==1.9.4
safety==3.8.1
pip==26.2.1
