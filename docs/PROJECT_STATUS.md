# DGAF/PDMAL Project Status

> **Current-status entrypoint:** use [`CURRENT_STATE.md`](CURRENT_STATE.md) for the live repository/evidence boundary.

This path is retained for compatibility and is not an independent current-state authority. Historical snapshots remain under `docs/historical/` and must not be read as live state unless explicitly promoted by a later governing record.

## Current hard boundary

As of the 2026-09-17 protected-main reconciliation through signed/verified commit `b1d91621bd73e70866d5ff8fd38fb98e440b30e9`:

- canonical High-Assurance program: **PRE-FREEZE / FAIL-CLOSED / NOT AUTHORIZED / AUTHORIZATION NOT GRANTED / N=0**;
- canonical DGAF efficacy: **NOT ESTABLISHED**;
- independent validation: **NOT ESTABLISHED**;
- Track A Epoch 001: prospective blinded collection complete, dataset lock established, protected mapping cryptographically unrecoverable, primary analysis unanalyzable/not run;
- Track A Epoch 002 collection: **COMPLETE** at 50 paired seed units / 2,250 blinded observations;
- successor dataset lock: **ESTABLISHED**;
- successor bounded unblinding: **AUTHORIZED** for `CONTROLLED_MAPPING_RELEASE_OR_DECRYPTION_ONLY`;
- successor materialization tooling: **ACCEPTED**;
- successor real materialization and immutable materialization receipt: **NOT ESTABLISHED**;
- successor prospective primary-analysis authorization tooling: **ACCEPTED / TOOLING ONLY**;
- successor primary analysis: **NOT AUTHORIZED / NOT RUN**;
- scientific-N increment: **0**.

## Accepted presentation state

The Governance Command Center now contains three accepted presentation-only Semantic Control Field tranches:

1. **Decision Frontier — PR #776**: current state, supporting evidence/provenance, blocker, nearest admissible transition, unreachable transitions, consequence preview, and receipt semantics.
2. **Governance Map — PR #779**: canonical vertical escalation, explicitly named lateral relationships, and global field constraints.
3. **State-Space Explorer V0 — PR #783**: discrete/categorical reachability derived from canonical stages. It distinguishes native predicate state from derived `established`, `frontier`, and `blocked_by_predecessor` regions.

State-Space V0 explicitly does **not** establish continuous/manifold coordinates, readiness percentages, distance-to-authorization, authorization probability, scalar evidence quality, efficacy gradients, or inferred consequence/reversibility values.

These interfaces are explanatory projections only. They do not create governance authority, authorize execution, establish deployment health, or change scientific state.

## Accepted repository assurance inventory

- **PR #780** introduced the bounded machine-readable assurance catalog and deterministic validation with `coverage.status = PARTIAL_CORE_FAMILIES_ONLY`.
- **PR #782** added deterministic workflow coverage-gap discovery for current workflow definitions not exactly represented by accepted catalog implementation paths.
- **PR #785** added seven source-verified recurring assurance families while preserving partial coverage.
- **PR #784** is closed unmerged and remains test-first/stale-lineage provenance only; its proposed workflow-role census is not accepted implementation.

Current protected-main required contexts remain separately **PPTL CI**, **Governance CI**, and **PR Issue-State Keyword Guard**. Catalog membership is not branch-protection requiredness. `UNMAPPED` / `UNCLASSIFIED` means not yet adjudicated, not “non-assurance.”

## Current scientific frontier

The next admissible scientific transition remains:

`controlled operator-side real materialization`
`→ validated non-secret materialization evidence admission`
`→ immutable materialization receipt`
`→ separate primary-analysis authorization`
`→ locked analysis`
`→ interpretation/adjudication`

The accepted apparatus provides Stage-1 materialization, Stage-2 evidence-bundle construction, and prospective primary-analysis authorization validation, but no current accepted record establishes real materialization, the materialization receipt, primary-analysis authorization, a primary result, efficacy, independent validation, or High-Assurance authorization.

Private keys, passphrases, protected plaintext mappings, blinding secrets, or other recoverable secret material must not be placed in GitHub, Notion, chat, CI inputs, workflow logs, or committed files.

## Presentation and runtime boundary

The Governance Command Center and ORBIT/Evidence Observer are presentation-layer companions to repository truth. A source/build/CI pass is not runtime health. A READY or failed deployment is not scientific authorization or empirical support. Runtime facts remain provider- and revision-bound.
