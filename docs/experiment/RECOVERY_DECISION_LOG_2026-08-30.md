# Recovery Decision Log — 2026-08-30

## Decision state

The current project objective is to reach a defensible first empirical observation without allowing historical-document uncertainty to become an unbounded work queue.

### Current disposition

- **P-31:** historical contract + implementation corroborated → proceed to bounded RESTORE design.
- **P-33:** historical contract + implementation corroborated → proceed to bounded RESTORE design.
- **P-32:** Production v1.0 historical contract + historical implementation context recovered → proceed to bounded RESTORE design.
- **P-27:** versioned historical component contract + implementation recovered → proceed to bounded R5 semantic binding; preserve exact version choice and do not silently mix v3.5/v3.6 semantics.
- **P-29:** historical implementation/integration lead recovered; exact normative contract still requires extraction.
- **P-30:** historical gate creation and attestation machinery recovered; exact normative acceptance contract still requires extraction.
- **DemiJoule:** historical specification/integration material recovered; exact six-axis gate contract still requires extraction.

## What is complete now

The project no longer needs another generic “search the repository for seven gate contracts” sweep. The evidence already located is sufficient to split the work into qualified gates and contract-extraction gates.

## What remains legitimately blocking N=1

The seven required TGL gates must be faithfully represented in the new candidate apparatus. For a gate without an exact recoverable contract, the gate remains FAIL-CLOSED; a proxy is not an allowed substitute.

After apparatus completion, the candidate identity must change. Historical P2/P6a evidence from earlier apparatus states does not transfer automatically.

## N=1 path

`contract qualification → bounded substrate design → implementation + preservation tests → canonical TGL binding → exact candidate SHA → fresh P2/P6a → required P3–P9 evidence → P9/independent checks → immutable freeze → explicit authorization → N=1`

## Loop-prevention checkpoint

Once each remaining gate reaches either:

- **EVIDENCE-QUALIFIED / RESTORE-READY**, or
- **CONTRACT EXTRACTION REQUIRED / FAIL-CLOSED**,

repeating discovery without new evidence is prohibited. New effort must produce either semantic extraction, preservation tests, apparatus changes, or candidate-verification evidence.
