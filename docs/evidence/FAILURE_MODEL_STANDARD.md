# DGAF Failure-Model Standard

Failure experiments must specify failure unit, sampling distribution, independence assumptions, timing, replacement/recovery behavior, and whether failures are observable to agents.

Primary experiments should distinguish random failures from targeted/adversarial failures. Paired stochastic comparisons must use identical failure realizations where the mapping is semantically valid; otherwise the protocol must document why identical node identities are not comparable.
