# Temporal SSOT Reconciliation — 2026-09-02

**Status:** CANONICAL TEMPORAL OVERLAY / NON-AUTHORIZING
**Purpose:** Reconcile current-state, timeline, progress, evidence, governance, taxonomy, and pattern-library projections without rewriting historical records.

## 1. Current control-plane identities

| Role | Current identity | State |
|---|---|---|
| GitHub `main` documentation/control-plane lineage | `275756fd81c975f17ae3d16d24e599db0617cf85` | CURRENT |
| Current experimental candidate under active remediation cycle | PR #192 / `edd3b5c8266e2680b9bb94301c2623a3f1ac0cf0` | CANDIDATE / PRE-FREEZE |
| Corrected apparatus provenance anchor | `2a54a67d84870e4eeb71b8aaf04413e0ca492ba1` | CANONICAL SOURCE ANCHOR |
| Prior mainline runtime candidate | `92ff830b1c67413df745e37087e6447c9c251b9a` | HISTORICAL / SCOPED |
| Prior controlled completion candidate | `a43219b4ed91fff8615f6c655ab3d17ca871fc29` | HISTORICAL / SCOPED |
| P-35 remediation lineage | PR #188 / prior heads | ENGINEERING / HISTORICAL AS APPLICABLE |

## 2. Non-substitution rules

`main`, candidate, apparatus source, deployment, workflow run, artifact, protocol/analysis binding, freeze, and authorization are distinct identity classes.

No document may infer current experimental candidacy merely because an identity is newer, appears in a later file, produced successful CI, has a READY deployment, or shares ancestry with the selected candidate.

Historical evidence remains valid only for its exact scope and cannot close a successor candidate.

## 3. Current experimental boundary

**PRE-FREEZE / FAIL-CLOSED / NOT AUTHORIZED / EMPIRICAL N = 0**.

No immutable freeze has been created for PR #192. No pilot authorization has been granted. No documentation, taxonomy, timeline, pattern registry, deployment, or CI result changes that boundary.

## 4. Critical dependency chain

`P-35 remediation acceptance -> exact candidate selection -> exact deployment binding -> fresh P2/P6a -> P3/P4/P5/P6 -> P7 -> P8 -> independent P9 -> freeze -> authorization -> empirical execution`

A dependency that changes the candidate identity invalidates downstream candidate-bound evidence unless explicitly re-executed and re-bound.

## 5. Cross-system agreement

A transversal state is coherent only when independently maintained projections resolve to the same scoped identity and compatible status.

Required tuple:

`apparatus/source SHA + candidate SHA/tree + deployment identity/source SHA + workflow/evidence run + artifact identity + protocol/analysis binding + freeze identity + authorization state`

Agreement classes:

- `ROLE DIFFERENCE` — intentional distinction between identities.
- `HISTORICAL DIFFERENCE` — retained prior state with explicit non-closing scope.
- `TRANSVERSAL DRIFT` — live projections can reasonably be read as different current states.
- `BLOCKING CONTRADICTION` — discrepancy capable of invalid evidence transfer or governance transition.

Current finding: GitHub and Notion agree on the non-authorizing/pre-freeze posture, but older current-candidate labels remain in historical/role-specific projections. Exact candidate convergence with Vercel is not established because no exact-source deployment is evidenced for PR #192 head `edd3b5c...`.

## 6. Timeline and progress-document rule

Timeline, progress, sprint, completion, historical-analysis, and visualization documents are historical records unless explicitly designated as live control surfaces.

Historical records must not be rewritten to simulate contemporaneous knowledge. Instead, they receive one of these treatments:

1. `HISTORICAL` — original snapshot preserved unchanged.
2. `HISTORICAL + RECONCILED` — original record preserved and a current temporal qualifier points here.
3. `CURRENT OVERLAY` — live state maintained separately from historical chronology.

The canonical current overlay is this document. Major operational Notion timelines should point to this overlay and retain their original historical measurements.

## 7. Layer coverage

This overlay governs synchronization semantics across:

- current-state / SSOT documents;
- P1–P9 evidence matrices;
- P2/P6a deployment handoffs;
- P8/P9 readiness and analysis-lock material;
- freeze/authorization controls;
- evidence indexes and claim indexes;
- taxonomy and epistemic vocabulary;
- agent/formation authority maps;
- NDR and DGAF pattern registries;
- Pattern Commons;
- repository architecture/readiness records;
- timeline/progress/history records;
- Notion operational and historical control surfaces.

## 8. Known temporal drift to resolve by role qualification

The following identities remain present in older repository files and are not to be interpreted as a contradiction merely because they differ from PR #192:

- `92ff830b...` — prior mainline runtime candidate;
- `a43219b...` — prior controlled completion candidate;
- `562753b...` — superseded completion candidate;
- prior PR #188 remediation heads;
- earlier freeze and deployment identities.

The required repair is role qualification and explicit historical status, not deletion of provenance.

## 9. Timeline integrity requirement

A timeline entry may report engineering progress, documentation progress, evidence progress, or experimental progress. These are different axes.

`engineering progress != evidence closure != experimental authorization != empirical progress`.

In particular, successful CI, documentation reconciliation, taxonomy updates, deployment health, or pre-freeze runner verification do not advance empirical N.

## 10. P-35 / P-42 vocabulary boundary

`P-35` = **Procluding Premise Gate**.

`P-42` = **Adaptive Harmonic Governance (AHG)**.

The explicit `premise_check_fn` required by the P-35 remediation is an engineering/wiring dependency at the DGAF/TGL/ConsensusTask boundary. It does not constitute approval of a PDMAL-specific constitutional premise policy.

## 11. Authoritative usage rule

When a current-state question conflicts with an older timeline or status snapshot:

1. resolve current GitHub live objects/checks;
2. resolve exact source/evidence artifacts;
3. use the current operational overlay;
4. retain older records as historical provenance;
5. never promote a historical identity merely because it appears in a later narrative.

**This document changes no experimental gate, creates no freeze, grants no authorization, and produces no empirical observation.**
