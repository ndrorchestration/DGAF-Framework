# P3–P6 Freeze-Readiness Engineering Record — 2026-08-21

## Status

**PRE-FREEZE / ENGINEERING CONTROLS REVIEWED**

This record separates implemented controls from evidence that still requires execution. It does not authorize empirical collection or a freeze.

## P3 — Artifact contract

Current canonical artifact validation is implemented in `experiments/pdmal_pilot/artifact_schema.py`.

The validator enforces:

- schema version;
- required document fields;
- PRE-FREEZE protocol status;
- prohibition of empirical collection in pre-freeze artifacts;
- non-empty records;
- required per-record provenance/outcome fields;
- constrained status vocabulary;
- exclusion/exclusion-reason consistency;
- non-empty environment fingerprint;
- 64-character hexadecimal artifact SHA-256.

The canonical sidecar format is deterministic and derived from the raw artifact bytes.

**Disposition:** implementation present; candidate-run artifact validation remains OPEN.

## P4 — Blinding/security

`PDMAL_BLINDING_KEY` is treated as an operational secret and the repository contains a dedicated synthetic-label operational verification procedure. That procedure explicitly prohibits production-secret access and requires evidence of mock-key separation, no cleartext leakage, successful post-freeze mock unblinding, and retention of the evidence record.

**Disposition:** control specification present; operational verification remains PENDING.

## P5 — Provenance/reproducibility

The current candidate deployment establishes the GitHub-to-Vercel source binding separately. Freeze evidence must bind the exact candidate SHA to protocol identity, runner/implementation identity, schema identity, configuration, deployment, environment, and artifact hashes.

Historical P6a evidence remains bounded to its original source/deployment and is not promoted to current-candidate evidence.

**Disposition:** provenance architecture present; candidate evidence packet remains OPEN.

## P6 — Durable evidence custody

Repository-local retention and sidecar hashing are not equivalent to durable research custody. A real custody event is required: archive the candidate evidence, retrieve it, recompute hashes, and record the round-trip result.

**Disposition:** OPEN pending actual archive/retrieval/hash event.

## Freeze boundary

No item above is promoted to VERIFIED solely because implementation exists. No empirical data are generated by this record. Empirical N remains `0`. Pilot authorization remains NOT GRANTED.
