name: DGAF Regression Suite
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      dgaf_url:
        description: 'Override DGAF_URL for live regression'
        required: false
        default: ''
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
jobs:
  local-quick-check:
    name: Local Quick Check (no network)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install httpx -q

      - name: Run quick_check.py
        run: python3 scripts/quick_check.py

      - name: Assert PSI cubic
        run: |
          python3 - <<'EOF'
          import sys
          sys.path.insert(0, '.')
          from resonant_decay.math_core import PSI, psi_cubic_check
          assert psi_cubic_check(), f'PSI cubic FAIL: PSI={PSI}'
          print(f'PSI cubic OK: PSI={PSI:.10f}')
          EOF
  live-regression:
    name: Live Regression (Vercel)
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch'
    env:
      DGAF_URL: >-
        ${{ inputs.dgaf_url ||
        vars.DGAF_URL ||
        'https://dgaf-framework.vercel.app' }}
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install httpx
        run: pip install httpx -q

      - name: Initialize verification state
        run: |
          printf '{"status":"NOT_EXECUTED","reason":"preflight pending","candidate_sha":"%s","run_id":"%s"}\n' "$GITHUB_SHA" "$GITHUB_RUN_ID" > regression_results_v17.json

      - name: Health preflight
        run: |
          set -euo pipefail
          curl -sf "$DGAF_URL/api/health" | python3 -m json.tool
          PSIOK=$(curl -sf "$DGAF_URL/api/health" | \
            python3 -c \
            "import sys,json; d=json.load(sys.stdin); print(d.get('psi_cubic',''))")
          [ "$PSIOK" = "True" ] \
            && echo 'psi_cubic OK' \
            || { echo 'psi_cubic FAIL'; exit 1; }

      - name: Mark preflight passed
        run: |
          python3 - <<'EOF'
          import json
          p='regression_results_v17.json'
          d=json.load(open(p))
          d.update(status='EXECUTION_READY', preflight='PASS')
          json.dump(d, open(p,'w'), indent=2, sort_keys=True)
          EOF

      - name: 30-turn live regression
        run: python3 scripts/live_regression_v17.py

      - name: Mark execution complete
        if: success()
        run: |
          python3 - <<'EOF'
          import json
          p='regression_results_v17.json'
          d=json.load(open(p))
          d.update(status='PASS', execution='PASS')
          json.dump(d, open(p,'w'), indent=2, sort_keys=True)
          EOF

      - name: Mark verification failure
        if: failure()
        run: |
          python3 - <<'EOF'
          import json
          p='regression_results_v17.json'
          d=json.load(open(p))
          if d.get('status') == 'NOT_EXECUTED':
              d.update(status='FAIL', verification='BLOCKED_OR_FAILED')
          else:
              d.update(status='FAIL', verification='FAILED')
          json.dump(d, open(p,'w'), indent=2, sort_keys=True)
          EOF

      - name: Upload regression results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regression-results-${{ github.run_id }}
          path: regression_results_v17.json
          if-no-files-found: error

      - name: Audit log check
        run: |
          set -euo pipefail
          curl -sf "$DGAF_URL/api/audit" | python3 -m json.tool
          TC=$(curl -sf "$DGAF_URL/api/audit" | \
            python3 -c \
            "import sys,json; print(json.load(sys.stdin).get('turn_count',0))")
          [ "$TC" -ge 30 ] \
            && echo "audit: turn_count=$TC OK" \
            || { echo "audit: turn_count=$TC < 30"; exit 1; }
