name: Track A Epoch 001 Final Closure

on:
  pull_request:
    branches: [main]
    paths:
      - 'scripts/validate_track_a_epoch_001_final_closure.py'
      - 'tests/test_track_a_epoch_001_final_closure.py'
      - '.github/workflows/track-a-epoch-001-final-closure.yml'
      - 'docs/experiment/track_a_runs/TRACK_A_EPOCH_001_FINAL_CLOSURE_PACKET.json'
      - 'docs/experiment/track_a_runs/TRACK_A_EPOCH_001_VERIFICATION_CLASSIFICATION.json'
      - 'docs/experiment/track_a_runs/TRACK_A_EPOCH_001_COLLECTION_AUTHORIZATION.json'
      - 'docs/experiment/track_a_runs/TRACK_A_EPOCH_001_IMMUTABLE_FREEZE_MANIFEST.json'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout exact PR source
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Prove checkout is exact PR head
        env:
          EXPECTED_HEAD: ${{ github.event.pull_request.head.sha }}
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD"

      - name: Set up Python 3.12.0
        uses: actions/setup-python@v5
        with:
          python-version: '3.12.0'

      - name: Run closure-validator mutation tests
        run: python -m unittest -v tests/test_track_a_epoch_001_final_closure.py

      - name: Validate tooling-only or exact one-file closure event
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          git fetch --no-tags --depth=1 origin "$BASE_SHA"
          closure='docs/experiment/track_a_runs/TRACK_A_EPOCH_001_FINAL_CLOSURE_PACKET.json'
          if test -e "$closure"; then
            python scripts/validate_track_a_epoch_001_final_closure.py
          else
            python scripts/validate_track_a_epoch_001_final_closure.py --expect-absent
            mapfile -t changed < <(git diff --name-only "$BASE_SHA" HEAD | sort)
            expected=(
              '.github/workflows/track-a-epoch-001-final-closure.yml'
              'scripts/validate_track_a_epoch_001_final_closure.py'
              'tests/test_track_a_epoch_001_final_closure.py'
            )
            mapfile -t expected_sorted < <(printf '%s\n' "${expected[@]}" | sort)
            test "${#changed[@]}" -eq "${#expected_sorted[@]}"
            diff -u <(printf '%s\n' "${expected_sorted[@]}") <(printf '%s\n' "${changed[@]}")
          fi

      - name: Prove downstream gates remain absent
        run: |
          test ! -e docs/experiment/track_a_runs/TRACK_A_EPOCH_001_VERIFICATION_CLASSIFICATION.json
          test ! -e docs/experiment/track_a_runs/TRACK_A_EPOCH_001_COLLECTION_AUTHORIZATION.json

      - name: Prove closure gate remains non-authorizing
        run: |
          echo 'TRACK_A_FREEZE = ESTABLISHED'
          echo 'TRACK_A_CLOSURE = TOOLING_OR_CLOSURE_VALIDATION_ONLY'
          echo 'TRACK_A_EMPIRICAL_EXECUTION = NOT_AUTHORIZED'
          echo 'SCIENTIFIC_N_INCREMENT = 0'
          echo 'CANONICAL_DGAF_EFFICACY = NOT_ESTABLISHED'
          echo 'HIGH_ASSURANCE = NOT_AUTHORIZED / N=0'
