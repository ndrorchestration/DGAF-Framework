[pytest]
testpaths = pptl/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    unit: pure-unit, no I/O
    integration: requires filesystem or network
    governance: validates DGAF gate contract
    slow: >1s execution
